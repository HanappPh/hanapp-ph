{
  "name": "@hanapp-ph/web",
  "sourceRoot": "apps/web",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "@hanapp-ph/web:build:development",
        "dev": true,
        "port": 4200
      },
      "configurations": {
        "development": {
          "buildTarget": "@hanapp-ph/web:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "@hanapp-ph/web:build:production",
          "dev": false
        }
      }
    },
    "start": {
      "executor": "@nx/next:server",
      "options": {
        "buildTarget": "@hanapp-ph/web:build:production",
        "dev": false,
        "port": 3000
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "@hanapp-ph/web:build:production"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/web/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/web/jest.config.cjs",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "coverageReporters": ["text"]
        }
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit --skipLibCheck",
        "cwd": "apps/web"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/.next"],
      "defaultConfiguration": "production",
      "options": {
        "command": "next build",
        "cwd": "apps/web"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    }
  },
  "tags": ["type:app", "scope:web"]
}
